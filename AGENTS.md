# AGENTS.md

## Repo Notes

- For `/Users/personal/realtime-codex-in-webstorm/voice-codex`, use `pnpm` instead of `npm`.
- Reason: `npm` is currently unreliable in this environment for that project, while `pnpm` works.

## voice-codex Commands

- Install deps: `pnpm install`
- Add deps: `pnpm add <package>`
- Run dev server: `pnpm dev`
- Build: `pnpm build`
